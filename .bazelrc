# Application Firmware - Bazel Configuration
# ===========================================

# Import Particle Bazel common configuration
import %workspace%/../particle-bazel/.bazelrc

# ============================================
# Application-specific overrides (if any)
# ============================================
